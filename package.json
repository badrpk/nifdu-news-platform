{
  "name": "nifdu-news-platform",
  "version": "1.0.0",
  "description": "News aggregation & editorial CMS with multi-source feeds — media product, not programming language.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test.js"
  },
  "engines": { "node": ">=18" }
}
